fn fib(n: Int): # write Fibonacci series up to n
    """Print a Fibonacci series up to n."""
    var a = 0
    var b = 1
    while a < n:
        print(a)
        a, b = b, a+b

fn main():
    fib(10)
